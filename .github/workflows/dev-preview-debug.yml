name: Dev-Preview Release

on:
  push:
    branches: [ "dev-preview" ]
  workflow_dispatch:

permissions:
  contents: write
  releases: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java (JDK 17)
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew :composeApp:assembleDebug
        env:
          KEYSTORE_PASSWORD: ${{ secrets.DEBUG_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.DEBUG_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.DEBUG_KEY_PASSWORD }}

      - name: Create Release
        uses: actions/create-release@v2
        id: create_release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: dev-preview
          release_name: "Dev-Preview Build (${GITHUB_SHA:0:7})"
          body: |
            ### 📱 自动构建预览版
            | 信息 | 值 |
            |------|-----|
            | 分支 | ${{ github.ref_name }} |
            | 提交 | [${GITHUB_SHA:0:7}](https://github.com/${{ github.repository }}/commit/${{ github.sha }}) |
            | 构建时间 | ${{ steps.get_timestamp.outputs.time }} |
          draft: false
          prerelease: true
          overwrite_assets: true

      - name: Upload APK
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_re
